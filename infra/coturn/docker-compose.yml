services:
  coturn:
    image: coturn/coturn:4
    command: ["-n", "--log-file=stdout", "--lt-cred-mech", "--use-auth-secret", "--static-auth-secret=${TURN_SECRET:-change-me}", "--realm=${TURN_REALM:-private-video-chat.local}"]
    ports:
      - "3478:3478/tcp"
      - "3478:3478/udp"
