{
  "name": "@private-video-chat/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@private-video-chat/shared": "workspace:*",
    "@preact/preset-vite": "^2.10.1",
    "@preact/signals": "^2.0.1",
    "preact": "^10.25.4",
    "vite": "^6.0.7"
  },
  "scripts": {
    "build": "vite build",
    "dev": "vite --host 0.0.0.0",
    "lint": "eslint src test --ext .ts,.tsx --max-warnings=0",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
