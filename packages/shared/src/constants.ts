export const PROTOCOL_VERSION = 1 as const;
export const LIMITS = {
  WS_FRAME_BYTES: 64 * 1024,
  ROOM_ID_CHARS: 22,
  PARTICIPANT_ID_CHARS: 22,
  RESUME_TOKEN_CHARS: 86,
  SENDER_ID_BYTES: 64,
  SESSION_NONCE_CHARS: 22,
  SENDER_PUBKEY_CHARS: 44,
  CIPHERTEXT_CHARS: 16 * 1024,
  IV_CHARS: 16,
  SIG_CHARS: 88,
  SDP_CHARS: 16 * 1024,
  CANDIDATE_CHARS: 1024,
  MAC_CHARS: 44,
  MSG_ID_CHARS: 36,
  UINT53_MAX: Number.MAX_SAFE_INTEGER,
} as const;
export const DEFAULTS = {
  JOIN_TIMEOUT_MS: 10_000,
  HEARTBEAT_MS: 25_000,
  IDLE_TIMEOUT_MS: 60_000,
  ACK_TIMEOUT_MS: 5_000,
  MAX_BACKPRESSURE_BYTES: 1024 * 1024,
  ROOM_GRACE_MS: 20_000,
  ROOM_TOMBSTONE_MS: 3 * 60_000,
  TURN_TTL_SEC: 600,
  MAX_GROUP_PARTICIPANTS: 50,
  MAX_CONNECTIONS: 10_000,
  MAX_ROOMS: 2_000,
  MAX_ROOMS_PER_IP: 20,
  CREATE_ROOMS_PER_IP: 20,
  JOINS_PER_IP: 60,
  CONNS_PER_IP: 20,
  RELAY_MSGS_PER_SEC: 10,
  RELAY_BYTES_PER_SEC: 64 * 1024,
  FANOUT_BYTES_PER_SEC: 512 * 1024,
  DEDUP_WINDOW: 1024,
  MAX_FILE_BYTES: 100 * 1024 * 1024,
  MAX_CONCURRENT_TRANSFERS: 2,
  CHUNK_SIZE: 16 * 1024,
  TRANSFER_STALL_MS: 30_000,
  BUFFER_HIGH: 1024 * 1024,
} as const;
export const ROOM_ID_BYTES = 16;
export const ROOM_ID_BASE64URL_LENGTH = 22;
export const E2E_KEY_BYTES = 32;
export const E2E_KEY_BASE64URL_LENGTH = 43;
export const SESSION_NONCE_BYTES = 16;
export const SESSION_NONCE_BASE64URL_LENGTH = 22;
export const MAX_GROUP_PARTICIPANTS = DEFAULTS.MAX_GROUP_PARTICIPANTS;
export const ROOM_GRACE_MS = DEFAULTS.ROOM_GRACE_MS;
export const ROOM_TOMBSTONE_MS = DEFAULTS.ROOM_TOMBSTONE_MS;
export const MAX_RELAY_CIPHERTEXT_BYTES = LIMITS.CIPHERTEXT_CHARS;
export const MAX_SENDER_ID_LENGTH = LIMITS.SENDER_ID_BYTES;
export const JOIN_TIMEOUT_MS = DEFAULTS.JOIN_TIMEOUT_MS;
export const BASE64URL_RE = /^[A-Za-z0-9_-]+$/;
export const ROOM_RE = /^[A-Za-z0-9_-]{22}$/;
export const UUID_V4_RE = /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{12}$/i;
